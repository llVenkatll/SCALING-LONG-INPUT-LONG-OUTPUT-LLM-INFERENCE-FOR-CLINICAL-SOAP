extends: systems_benchmark_serving_stack_comparison.yaml

experiment:
  name: systems_benchmark_serving_stack_long_context
  output_dir: ${CLINICAL_SPEECH_OUTPUTS_DIR}/systems_benchmark_serving_stack_long_context
  log_dir: ${CLINICAL_SPEECH_LOGS_DIR}/systems_benchmark_serving_stack_long_context
  benchmark_dir: ${CLINICAL_SPEECH_BENCHMARKS_DIR}/systems_benchmark_serving_stack_long_context
  profiler_dir: ${CLINICAL_SPEECH_PROFILER_DIR}/systems_benchmark_serving_stack_long_context
  checkpoint_dir: ${CLINICAL_SPEECH_CHECKPOINTS_DIR}/systems_benchmark_serving_stack_long_context

dataset:
  path: /data/project_runtime/datasets/medsynth/long_context_4000.jsonl

systems_benchmark:
  num_prompts: 2
  batch_sizes: [1, 2]
  max_new_tokens: 16
