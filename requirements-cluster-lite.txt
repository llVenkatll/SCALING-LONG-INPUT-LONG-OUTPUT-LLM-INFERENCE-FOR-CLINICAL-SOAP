PyYAML>=6.0
tqdm>=4.66.0
pandas>=2.2.0
numpy>=1.26.0
transformers>=4.40.0
accelerate>=0.30.0
sentencepiece>=0.2.0
evaluate>=0.4.0
bert-score>=0.3.13
rouge-score>=0.1.2
jiwer>=3.0.0
pydantic>=2.6.0

# Intentionally excludes:
# - torch
# - bitsandbytes
#
# Use this on quota-limited clusters when PyTorch is already available
# through a shared environment/module, or when you want to install the
# lighter Python stack first and handle PyTorch separately in a scratch-based env.
